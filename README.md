# LocationShareApp
A location sharing application
Steps:

```sh
#Clone the repo:
$ git clone https://github.com/gouravmodi/LocationShareApp.git

$ bundle install

$ rake db:create

$ rake db:seed

$ rails s
```